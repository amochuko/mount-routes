{
  "name": "@numquid/route-mounter",
  "version": "1.0.0-fixed.0",
  "description": "Utility that mounts routers in an Express.js app",
  "main": "dist/mounter.js",
  "module": "dist/mounter.js",
  "types": "dist/mounter.d.ts",
  "exports": {
    ".": {
      "import": "./dist/mounter.js",
      "require": "./dist/mounter.js",
      "types": "./dist/mounter.d.ts"
    }
  },
  "private": false,
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "npm run clean && tsc --build",
    "prepare": "npm run build",
    "start": "npm run build && node dist/server.js",
    "dev": "NODE_ENV=development nodemon --watch 'src' --ext ts --exec 'ts-node' src/server.ts",
    "test": "jest",
    "test:coverage": "jest --forceExit --coverage --verbose",
    "profiler": "NODE_ENV=production node --prof dist/server.js",
    "lint": "npx eslint .",
    "prepublishOnly": "npm run build",
    "release:patch": "npm run build && npm version patch -m 'chore: release v%s' && git push --follow-tags",
    "release:minor": "npm run build && npm version minor -m 'chore: release v%s' && git push --follow-tags",
    "release:major": "npm run build && npm version major -m 'chore: release v%s' && git push --follow-tags"
  },
  "author": "Ochuko <7204868+amochuko@users.noreply.github.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/amochuko/mount-routes/issues"
  },
  "keywords": [
    "route",
    "routers",
    "mounting",
    "experss mounter",
    "express.js",
    "nodejs"
  ],
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.7",
    "@types/supertest": "^6.0.3",
    "@typescript-eslint/parser": "^8.46.2",
    "eslint": "^9.19.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.9",
    "supertest": "^7.1.4",
    "ts-jest": "^29.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.23.0"
  }
}
